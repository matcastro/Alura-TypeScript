import React, { Component } from "react";

class Botao extends Component {
    render() {
        return (
            <button>
                Botão
            </button>
        )
    }
}

export default Botao